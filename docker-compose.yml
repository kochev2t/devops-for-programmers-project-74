services:
  app:
    image: kochev2t/devops-for-programmers-project-74:latest
    build:
      context: .
      dockerfile: Dockerfile.production
    volumes:
      - ./app:/app
    command: make test